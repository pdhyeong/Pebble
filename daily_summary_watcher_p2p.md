# Pebble 일일 작업 요약 (파일 와처 및 동기화 안정화)

오늘 진행된 주요 개발 및 버그 수정 사항을 요약한 문서입니다. `Pebble`의 실시간 파일 동기화 기능과 UI 이벤트 처리의 안정성이 크게 개선되었습니다.

## 1. 실시간 파일 감시자 (File Watcher) 구현
- **배경**: 공유 폴더 내에서 발생하는 파일 생성, 수정 등의 이벤트를 감지하여 P2P 동기화 및 UI 새로고침을 수행하기 위함.
- **구현 내용**:
  - Rust의 `notify` 크레이트를 활용하여 `watcher.rs` 모듈 신규 구현.
  - 디렉토리의 변경 사항을 디바운스(Debounce) 처리하여 중복 이벤트를 방지.
  - `App.tsx` 초기 로딩 시와 `설정 모달`에서 공유 경로 변경 시 백그라운드 감시자를 재시작하도록 프론트엔드 연동.
  - `LocalFilesView` 컴포넌트에서 `local-files-changed` 이벤트를 리스닝하여 파일 변동 시 로컬 파일 목록 UI 최신화.

## 2. 파일 자동 동기화 (P2P Push) 적용
- **구현 내용**:
  - 로컬에서 파일이 추가되거나 변경되면, 현재 온라인 상태로 P2P 연결된 신뢰할 수 있는 피어들에게 해당 파일을 자동 전송(`PushFileToConnectedPeers` 커맨드).
  - 절대 경로 대신 **공유 폴더 기준 상대 경로**를 추출해 상대방에게 전달함으로써 서로 다른 OS 및 저장 경로 설정을 가진 환경 간의 원활한 동기화 지원.

## 3. 핵심 버그 및 구조적 결함 수정

### A. 무한 에코(Ping-Pong) 전송 버그 해결 (macOS 경로 특성 대응)
- **증상**: P2P로 파일을 수신받고 저장하면, 내 로컬 Watcher가 이를 '새로운 로컬 파일 생성'으로 간주해 상대방에게 다시 전송하는 무한 루프 발생.
- **해결**: 
  - 방금 수신한 파일을 1시간 동안 감시 이그노어 리스트(`watcher_ignore_list`)에 등록하는 로직이 있었으나, macOS의 심볼릭 링크 및 `PathBuf` 엄격한 타입 비교 문제로 경로 매칭이 실패하던 상황 판명.
  - 이그노어 리스트의 자료형을 순수 문자열(`String`) 기반 맵으로 모두 교체하여 운영체제 간 경로 해석 차이로 인한 매칭 실패를 원천 차단.

### B. "좀비 파일 진행바" UI 버그 해결 (상태 및 이벤트 꼬임)
- **증상 1 (수신 측 무반응)**: 파일을 누군가 나에게 전송하기 시작할 때 수신 측 UI에 즉시 다운로드 바가 뜨지 않고 용량 계산이 꼬이는 현상.
- **증상 2 (X버튼 작동안함)**: 업로드나 다운로드가 100% 완료되거나 중간에 실패, 취소되어도 진행 UI가 사라지지 않고 영구적으로 남는 현상.
- **해결**:
  - 프론트엔드 (`useP2pEventListeners.ts`): 에러 발생 시 모든 전송 바(`Map`)를 초기화하던 로직을 수정하여, 고유 `transfer_id`를 통해 해당 프로그레스 바만 선택적으로 삭제하도록 상태 관리 로직 재편.
  - 백엔드 (`handlers.rs` 수신 측): `FileTransferStreamMsg::Init` (스트림 초기화 단계)에서 `file-download-started` 이벤트를 의도적으로 프론트로 방출하도록 추가하여, 최초 수신 시점부터 총 용량(`total_size`)과 `transfer_id`를 프론트엔드가 인지하도록 수정.
  - 백엔드 (`handlers.rs` 완료 측): 다운로드 100% 도달 시 기존엔 `file-received` 이벤트만 쏘고 있었으나, UI 정리를 위한 `file-download-complete` 이벤트도 함께 방출하도록 누락된 코드 복구.

## 4. 향후 고려사항 (User Comment 기준)
- **동기화 전략에 대한 고민**: 
  - "서로의 변경 사항을 100% 미러링 할지(자동 공유)" vs "온디맨드로 필요한 파일만 리스트에서 땡겨올지(수동 다운로드)" 정책 결정 중.
  - 현재는 **'파일을 수정하면 상대방 폴더에도 동일 상대 경로로 자동 업데이트'** 되는 로직이 포함된 상태이나, 향후 Pebble Server를 통한 온디맨드 구조로 정책이 확정되면 P2P Push 자동화 부분을 걷어내고 메타데이터 기반 수동 동기화 창으로 대체할 예정입니다.
